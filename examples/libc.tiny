
/**
# How should pointers work in tiny lang?

- i8* will be a pointer type. it'll point to a memory location.

**/

extern fun malloc(a: *i8) => *i8;
extern fun free(a: *i8) => void;
extern fun memcpy(a: *i8, b: *i8, c: isize) => *i8;

fun main() => isize {
    return 0;
}
