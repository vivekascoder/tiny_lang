/**
* Demostration of while loop in tiny lang.
**/

fun main() => usize {
    let i: usize = 0;
    while (i < 5) {
        // print(i);
        // print('\n');
        i = i + 1;
    }
    return 0;
}